# Configuration for Compute API Tests
#
# For local testing:
#   1. Copy this file: cp test/.env.example test/.env
#   2. Update the values below with your actual configuration
#
# Or use environment-specific files if available:
#   - For dev: cp test/.env.dev test/.env
#   - For UAT: cp test/.env.uat test/.env

# Base URL for the Compute API server
API_BASE_URL=http://localhost:8080

# Base URL for the Identity API server (used for quota checks)
IDENTITY_BASE_URL=http://localhost:8081

# Authentication token for API requests
# This is a service token, which you can create on console, no need to add bearer, it will add it for you
API_AUTH_TOKEN=your-test-token-here

# Test data configuration

# Base URL for the region service, which is needed in some cases to clean things up.
REGION_BASE_URL=http://localhost:8082

# Advised you create all the below in console first and add them all here
TEST_ORG_ID=test-org-12345
TEST_PROJECT_ID=test-project-67890
TEST_SECONDARY_PROJECT_ID=test-project-secondary-12345

# You can call the /api/v1/organizations/{{organizationID}}/regions/{{regionId}}/flavors & /api/v1/organizations/{{organizationID}}/regions endpoints
TEST_REGION_ID=test-region-abc123
TEST_SECONDARY_REGION_ID=test-region-secondary-abc123
TEST_NETWORK_ID=test-network-jkl012

# Shared test data
TEST_FLAVOR_ID=test-flavor-def456
TEST_IMAGE_ID=test-image-ghi789

# Test timeout settings
REQUEST_TIMEOUT=30s
TEST_TIMEOUT=20m

# Test behavior flags
SKIP_INTEGRATION=false

# Debug settings
DEBUG_LOGGING=false
LOG_REQUESTS=true
LOG_RESPONSES=false
